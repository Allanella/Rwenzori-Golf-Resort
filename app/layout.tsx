import type { Metadata } from 'next'
import { Geist, Geist_Mono } from 'next/font/google'
import { Analytics } from '@vercel/analytics/next'
import './globals.css'

const _geist = Geist({ subsets: ["latin"] });
const _geistMono = Geist_Mono({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: 'Mt. Rwenzori Golf Resort & Spa | Luxury Mountain Retreat',
  description: 'Experience luxury at Mt. Rwenzori Golf Resort & Spa. Serene mountain retreat with 9-hole golf course, premium spa services, English breakfast, and breathtaking views.',
  generator: 'Next.js',
  keywords: ['golf resort', 'spa', 'luxury accommodation', 'mountain retreat', 'Rwenzori'],
  openGraph: {
    title: 'Mt. Rwenzori Golf Resort & Spa',
    description: 'A serene mountain retreat with golf, spa, and luxury accommodation',
    type: 'website',
  },
  icons: {
    icon: [
      {
        url: '/icon-light-32x32.png',
        media: '(prefers-color-scheme: light)',
      },
      {
        url: '/icon-dark-32x32.png',
        media: '(prefers-color-scheme: dark)',
      },
      {
        url: '/icon.svg',
        type: 'image/svg+xml',
      },
    ],
    apple: '/apple-icon.png',
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" className="bg-background">
      <body className="font-sans antialiased bg-background text-foreground">
        {children}
        {process.env.NODE_ENV === 'production' && <Analytics />}
      </body>
    </html>
  )
}
